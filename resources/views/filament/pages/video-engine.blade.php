<x-filament-panels::page>
    <style>
        [x-cloak]{display:none!important}.ve{display:grid;gap:13px;max-width:1440px}.ve-panel,.ve-head{border:1px solid rgba(148,163,184,.18);background:linear-gradient(145deg,rgba(2,6,23,.88),rgba(15,23,42,.76));border-radius:18px}.ve-head{padding:15px 17px;display:flex;align-items:center;justify-content:space-between;gap:16px}.ve-kicker{font-size:10px;font-weight:950;letter-spacing:.12em;color:#67e8f9}.ve-title{font-size:22px;font-weight:950;color:#fff;line-height:1.1}.ve-note{font-size:12px;color:#94a3b8;line-height:1.4}.ve-head-side,.ve-actions,.ve-meta,.ve-toolbar,.ve-tabs,.ve-editor-tabs,.ve-view{display:flex;align-items:center;gap:7px;flex-wrap:wrap}.ve-head-side{justify-content:flex-end}.ve-app{padding:7px 10px;border-radius:11px;border:1px solid rgba(34,211,238,.26);background:rgba(34,211,238,.08)}.ve-app small{display:block;font-size:8px;letter-spacing:.1em;color:#67e8f9}.ve-app strong{font-size:11px;color:#fff}.ve-btn,.ve-tabs button,.ve-editor-tabs button,.ve-view button{border:1px solid rgba(148,163,184,.22);border-radius:10px;background:rgba(30,41,59,.72);color:#e2e8f0;padding:8px 10px;font-size:11px;font-weight:900;cursor:pointer;white-space:nowrap}.ve-btn.primary,.ve-tabs button.active,.ve-editor-tabs button.active,.ve-view button.active{border-color:rgba(34,211,238,.55);background:rgba(8,145,178,.38);color:#fff}.ve-tabs,.ve-editor-tabs{overflow-x:auto;flex-wrap:nowrap;padding:5px;border:1px solid rgba(148,163,184,.15);border-radius:13px;background:rgba(2,6,23,.45)}.ve-panel{padding:14px}.ve-panel-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:11px}.ve-panel-title{font-size:15px;font-weight:950;color:#fff}.ve-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:9px}.ve-stat,.ve-card{border:1px solid rgba(148,163,184,.16);border-radius:13px;background:rgba(30,41,59,.45)}.ve-stat{padding:13px}.ve-stat strong{display:block;font-size:24px;color:#fff}.ve-toolbar{justify-content:space-between}.ve-filters{display:flex;gap:7px;flex-wrap:wrap;flex:1}.ve-input,.ve-select,.ve-textarea{width:100%;border:1px solid rgba(148,163,184,.24);border-radius:10px;background:#0f172a;color:#f8fafc;padding:9px 10px;font-size:12px}.ve-filters .ve-input{max-width:260px}.ve-filters .ve-select{width:auto;min-width:130px}.ve-library{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin-top:11px}.ve-library.list{grid-template-columns:1fr}.ve-card{overflow:hidden;display:grid}.ve-library.list .ve-card{grid-template-columns:120px 1fr auto;align-items:center}.ve-thumb{height:130px;background:linear-gradient(135deg,#0c4a6e,#312e81);display:grid;place-items:center;color:#bae6fd;font-size:34px;font-weight:950;overflow:hidden}.ve-thumb img{width:100%;height:100%;object-fit:cover}.ve-library.list .ve-thumb{height:92px}.ve-card-body{padding:11px;display:grid;gap:7px}.ve-card-title{font-size:13px;font-weight:950;color:#fff}.ve-badge{display:inline-flex;padding:3px 7px;border-radius:999px;background:rgba(34,211,238,.10);border:1px solid rgba(34,211,238,.18);color:#a5f3fc;font-size:9px;font-weight:900}.ve-card-action{padding:0 11px 11px}.ve-library.list .ve-card-action{padding:11px}.ve-empty{padding:28px;text-align:center;border:1px dashed rgba(148,163,184,.25);border-radius:13px;color:#94a3b8}.ve-editor{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.65fr);gap:12px;align-items:start}.ve-main,.ve-preview{border:1px solid rgba(148,163,184,.16);border-radius:15px;background:rgba(2,6,23,.42);padding:13px}.ve-preview{position:sticky;top:78px}.ve-form{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:11px;margin-top:11px}.ve-wide{grid-column:1/-1}.ve-label{display:block;margin-bottom:5px;font-size:10px;font-weight:900;color:#cbd5e1}.ve-check{display:flex;gap:8px;align-items:center;color:#e2e8f0;font-size:11px}.ve-preview-thumb{height:190px;border-radius:13px;background:linear-gradient(135deg,#0e7490,#312e81);display:grid;place-items:center;overflow:hidden;color:#cffafe;font-size:44px}.ve-preview-thumb img{width:100%;height:100%;object-fit:cover}.ve-preview h3{margin:11px 0 8px;color:#fff;font-weight:950;font-size:17px}.ve-preview-row{display:flex;justify-content:space-between;gap:12px;padding:7px 0;border-bottom:1px solid rgba(148,163,184,.12);font-size:10px}.ve-preview-row span{color:#94a3b8}.ve-preview-row strong{color:#e2e8f0;text-align:right}.ve-members{display:grid;grid-template-columns:1fr 1fr;gap:10px}.ve-member-list{display:grid;gap:6px;max-height:390px;overflow-y:auto}.ve-member{display:grid;grid-template-columns:1fr auto;gap:7px;align-items:center;padding:8px;border:1px solid rgba(148,163,184,.16);border-radius:10px;background:rgba(30,41,59,.38);font-size:11px;color:#fff}.ve-member-actions{display:flex;gap:4px}.ve-member-actions button{padding:4px 6px;border-radius:7px;border:1px solid rgba(148,163,184,.2);background:#1e293b;color:#fff;font-size:9px}.ve-code{max-height:260px;overflow:auto;padding:10px;border-radius:10px;background:#020617;color:#bae6fd;font-size:9px}.ve-error{font-size:10px;color:#fca5a5;margin-top:4px}@media(max-width:1050px){.ve-library{grid-template-columns:repeat(2,minmax(0,1fr))}.ve-editor{grid-template-columns:1fr}.ve-preview{position:static}.ve-members{grid-template-columns:1fr}}@media(max-width:680px){.ve-head{align-items:flex-start;flex-direction:column}.ve-head-side{justify-content:flex-start}.ve-stats,.ve-library,.ve-form{grid-template-columns:1fr 1fr}.ve-wide{grid-column:1/-1}.ve-library.list .ve-card{grid-template-columns:90px 1fr}.ve-library.list .ve-card-action{grid-column:1/-1;padding-top:0}}@media(max-width:480px){.ve-stats,.ve-library,.ve-form{grid-template-columns:1fr}.ve-wide{grid-column:auto}.ve-head-side .ve-btn{padding:7px 8px}.ve-preview-thumb{height:155px}}
    </style>

    <div class="ve" x-data="{keep(){sessionStorage.setItem('video_engine_scroll_y',String(window.scrollY||0))},restore(){let y=Number(sessionStorage.getItem('video_engine_scroll_y')||0);if(y>0)setTimeout(()=>window.scrollTo({top:y,behavior:'instant'}),70)}}" x-init="restore()" @beforeunload.window="keep()" @video-engine-keep-position.window="keep();$nextTick(()=>restore())">
        <header class="ve-head">
            <div><div class="ve-kicker">VIDEO ENGINE</div><div class="ve-title">Long-form video workspace</div><div class="ve-note">Channels, playlists and playback.</div></div>
            <div class="ve-head-side">
                <div class="ve-app"><small>ACTIVE APP</small><strong>{{ $activeAppId ? ($activeAppName ?: 'App #'.$activeAppId) : 'Not selected' }}</strong></div>
                @if($activeAppId)<button class="ve-btn primary" wire:click="createVideo">+ Video</button><button class="ve-btn" wire:click="createChannel">+ Channel</button><button class="ve-btn" wire:click="createPlaylist">+ Playlist</button>@endif
            </div>
        </header>

        <nav class="ve-tabs">@foreach(['overview'=>'Overview','videos'=>'Videos','channels'=>'Channels','playlists'=>'Playlists','preview'=>'Preview'] as $key=>$label)<button class="{{ $activeTab===$key?'active':'' }}" wire:click="selectTab('{{ $key }}')">{{ $label }}</button>@endforeach</nav>

        @if(!$activeAppId)
            <section class="ve-panel"><div class="ve-empty"><strong>Select an active app.</strong><br>Video Engine does not fall back to another app.</div></section>
        @elseif($workspaceMode !== 'index')
            @php
                $editorTabs = match($workspaceMode){'video_editor'=>['details'=>'Details','source'=>'Source','media'=>'Media','publishing'=>'Publishing','preview'=>'Preview'],'channel_editor'=>['details'=>'Details','media'=>'Media','publishing'=>'Publishing','preview'=>'Preview'],default=>['details'=>'Details','videos'=>'Videos','publishing'=>'Publishing','preview'=>'Preview']};
                $preview = $this->preview;
            @endphp
            <section class="ve-panel">
                <div class="ve-panel-head"><div><div class="ve-panel-title">{{ str_replace('_editor','',ucfirst($workspaceMode)) }} editor</div><div class="ve-note">{{ $editingVideoId||$editingChannelId||$editingPlaylistId?'Editing existing record':'New record' }}</div></div><button class="ve-btn" wire:click="closeEditor">Back</button></div>
                <div class="ve-editor-tabs">@foreach($editorTabs as $key=>$label)<button class="{{ $editorTab===$key?'active':'' }}" wire:click="selectEditorTab('{{ $key }}')">{{ $label }}</button>@endforeach</div>
                <div class="ve-editor" style="margin-top:11px">
                    <div class="ve-main">
                        @if($workspaceMode === 'video_editor')
                            @if($editorTab === 'details')<div class="ve-form"><div><label class="ve-label">Channel</label><select class="ve-select" wire:model.defer="videoForm.video_channel_id"><option value="">Select</option>@foreach($this->channelOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div><div><label class="ve-label">Title</label><input class="ve-input" wire:model.defer="videoForm.title"></div><div><label class="ve-label">Slug</label><input class="ve-input" wire:model.defer="videoForm.slug" placeholder="Auto from title"></div><div class="ve-wide"><label class="ve-label">Description</label><textarea class="ve-textarea" rows="4" wire:model.defer="videoForm.description"></textarea></div></div>@endif
                            @if($editorTab === 'source')<div class="ve-form"><div><label class="ve-label">Source type</label><select class="ve-select" wire:model.live="videoForm.source_type">@foreach(['uploaded_video'=>'Uploaded video','external_video'=>'External video','hls'=>'HLS','youtube_video'=>'YouTube video','web_embed'=>'Web embed'] as $key=>$label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select></div><div><label class="ve-label">Provider</label><input class="ve-input" wire:model.defer="videoForm.provider"></div>@if($videoForm['source_type']==='youtube_video')<div><label class="ve-label">YouTube video ID</label><input class="ve-input" wire:model.defer="videoForm.provider_video_id"></div><div><label class="ve-label">YouTube URL</label><input class="ve-input" wire:model.defer="videoForm.external_url"></div>@elseif($videoForm['source_type']!=='uploaded_video')<div class="ve-wide"><label class="ve-label">{{ $videoForm['source_type']==='hls'?'HLS URL':($videoForm['source_type']==='web_embed'?'Embed URL':'Video URL') }}</label><input class="ve-input" type="url" wire:model.defer="videoForm.external_url"></div>@else<div class="ve-wide ve-note">Select the uploaded asset under Media.</div>@endif</div>@endif
                            @if($editorTab === 'media')<div class="ve-form">@if($videoForm['source_type']==='uploaded_video')<div class="ve-wide"><label class="ve-label">Video asset</label><select class="ve-select" wire:model.defer="videoForm.media_asset_id"><option value="">Select</option>@foreach($this->videoMediaOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div>@endif<div><label class="ve-label">Thumbnail</label><select class="ve-select" wire:model.defer="videoForm.thumbnail_media_asset_id"><option value="">None</option>@foreach($this->imageMediaOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div><div><label class="ve-label">MIME type</label><input class="ve-input" wire:model.defer="videoForm.mime_type"></div><div><label class="ve-label">Duration seconds</label><input class="ve-input" type="number" min="0" wire:model.defer="videoForm.duration_seconds"></div><div><label class="ve-label">Aspect ratio</label><input class="ve-input" wire:model.defer="videoForm.aspect_ratio" placeholder="16:9"></div></div>@endif
                            @if($editorTab === 'publishing')<div class="ve-form"><div><label class="ve-label">Status</label><select class="ve-select" wire:model.defer="videoForm.status"><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></div><div><label class="ve-label">Visibility</label><select class="ve-select" wire:model.defer="videoForm.visibility"><option value="public">Public</option><option value="unlisted">Unlisted</option><option value="private">Private</option></select></div><div><label class="ve-label">Order</label><input class="ve-input" type="number" min="0" wire:model.defer="videoForm.sort_order"></div><label class="ve-check"><input type="checkbox" wire:model.defer="videoForm.is_featured"> Featured</label><label class="ve-check"><input type="checkbox" wire:model.defer="videoForm.is_live"> Live</label><label class="ve-check"><input type="checkbox" wire:model.defer="videoForm.embed_allowed"> Embed allowed</label><label class="ve-check"><input type="checkbox" wire:model.defer="videoForm.external_open_allowed"> External open</label></div>@endif
                        @elseif($workspaceMode === 'channel_editor')
                            @if($editorTab === 'details')<div class="ve-form"><div><label class="ve-label">Title</label><input class="ve-input" wire:model.defer="channelForm.title"></div><div><label class="ve-label">Slug</label><input class="ve-input" wire:model.defer="channelForm.slug" placeholder="Auto from title"></div><div class="ve-wide"><label class="ve-label">Description</label><textarea class="ve-textarea" rows="4" wire:model.defer="channelForm.description"></textarea></div></div>@endif
                            @if($editorTab === 'media')<div class="ve-form"><div class="ve-wide"><label class="ve-label">Thumbnail</label><select class="ve-select" wire:model.defer="channelForm.thumbnail_media_asset_id"><option value="">None</option>@foreach($this->imageMediaOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div></div>@endif
                            @if($editorTab === 'publishing')<div class="ve-form"><div><label class="ve-label">Status</label><select class="ve-select" wire:model.defer="channelForm.status"><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></div><div><label class="ve-label">Visibility</label><select class="ve-select" wire:model.defer="channelForm.visibility"><option value="public">Public</option><option value="unlisted">Unlisted</option><option value="private">Private</option></select></div><div><label class="ve-label">Order</label><input class="ve-input" type="number" min="0" wire:model.defer="channelForm.sort_order"></div><label class="ve-check"><input type="checkbox" wire:model.defer="channelForm.is_featured"> Featured</label></div>@endif
                        @else
                            @if($editorTab === 'details')<div class="ve-form"><div><label class="ve-label">Channel</label><select class="ve-select" wire:model.defer="playlistForm.video_channel_id"><option value="">Select</option>@foreach($this->channelOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div><div><label class="ve-label">Title</label><input class="ve-input" wire:model.defer="playlistForm.title"></div><div><label class="ve-label">Slug</label><input class="ve-input" wire:model.defer="playlistForm.slug" placeholder="Auto from title"></div><div><label class="ve-label">Provider</label><input class="ve-input" wire:model.defer="playlistForm.provider"></div><div><label class="ve-label">Provider playlist ID</label><input class="ve-input" wire:model.defer="playlistForm.provider_playlist_id"></div><div><label class="ve-label">External URL</label><input class="ve-input" type="url" wire:model.defer="playlistForm.external_url"></div><div class="ve-wide"><label class="ve-label">Description</label><textarea class="ve-textarea" rows="3" wire:model.defer="playlistForm.description"></textarea></div></div>@endif
                            @if($editorTab === 'videos')<div class="ve-members"><div><label class="ve-label">Available videos</label><input class="ve-input" wire:model.live.debounce.300ms="playlistVideoSearch" placeholder="Search"><div class="ve-member-list" style="margin-top:7px">@forelse($this->availableVideoOptions as $id=>$label)<div class="ve-member"><span>{{ $label }}</span><button class="ve-btn" wire:click="togglePlaylistVideo({{ $id }})">{{ in_array((int)$id,array_map('intval',$playlistForm['video_ids']??[]),true)?'Remove':'Add' }}</button></div>@empty<div class="ve-empty">No matching videos.</div>@endforelse</div></div><div><label class="ve-label">Selected · ordered</label><div class="ve-member-list">@forelse($this->selectedPlaylistVideos as $video)<div class="ve-member"><span>{{ $loop->iteration }}. {{ $video->title }}</span><div class="ve-member-actions"><button wire:click="movePlaylistVideo({{ $video->id }},'up')">↑</button><button wire:click="movePlaylistVideo({{ $video->id }},'down')">↓</button><button wire:click="togglePlaylistVideo({{ $video->id }})">×</button></div></div>@empty<div class="ve-empty">No videos selected.</div>@endforelse</div></div></div>@endif
                            @if($editorTab === 'publishing')<div class="ve-form"><div><label class="ve-label">Thumbnail</label><select class="ve-select" wire:model.defer="playlistForm.thumbnail_media_asset_id"><option value="">None</option>@foreach($this->imageMediaOptions as $id=>$label)<option value="{{ $id }}">{{ $label }}</option>@endforeach</select></div><div><label class="ve-label">Status</label><select class="ve-select" wire:model.defer="playlistForm.status"><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select></div><div><label class="ve-label">Visibility</label><select class="ve-select" wire:model.defer="playlistForm.visibility"><option value="public">Public</option><option value="unlisted">Unlisted</option><option value="private">Private</option></select></div><div><label class="ve-label">Order</label><input class="ve-input" type="number" min="0" wire:model.defer="playlistForm.sort_order"></div><label class="ve-check"><input type="checkbox" wire:model.defer="playlistForm.is_featured"> Featured</label></div>@endif
                        @endif
                        @if($editorTab === 'preview')<div class="ve-note">Canonical payload readiness</div><pre class="ve-code">{{ json_encode($preview,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre>@endif
                        <div class="ve-actions" style="margin-top:13px"><button class="ve-btn primary" x-on:click="keep()" wire:click="{{ $workspaceMode==='video_editor'?'saveVideo':($workspaceMode==='channel_editor'?'saveChannel':'savePlaylist') }}">Save</button><button class="ve-btn" wire:click="closeEditor">Close</button></div>
                    </div>
                    <aside class="ve-preview"><div class="ve-preview-thumb">@if($preview['thumbnail_url'])<img src="{{ $preview['thumbnail_url'] }}" alt="">@else▶@endif</div><h3>{{ $preview['title'] }}</h3><div class="ve-meta"><span class="ve-badge">{{ ucfirst($preview['type']) }}</span><span class="ve-badge">{{ $preview['ready']?'Ready':'Needs setup' }}</span></div>@foreach($preview['meta'] as $label=>$value)<div class="ve-preview-row"><span>{{ ucfirst((string)$label) }}</span><strong>{{ is_scalar($value)?$value:json_encode($value) }}</strong></div>@endforeach@if($preview['playback'])<div class="ve-preview-row"><span>Playback</span><strong>{{ $preview['playback']['source_type']??'Ready' }}</strong></div>@endif</aside>
                </div>
            </section>
        @elseif($activeTab === 'overview')
            <section class="ve-panel"><div class="ve-panel-head"><div><div class="ve-panel-title">Overview</div><div class="ve-note">{{ $activeAppName }}</div></div></div><div class="ve-stats">@foreach(['channels'=>'Channels','playlists'=>'Playlists','videos'=>'Videos','published'=>'Published'] as $key=>$label)<div class="ve-stat"><strong>{{ $this->overview[$key] }}</strong><span class="ve-note">{{ $label }}</span></div>@endforeach</div></section>
        @elseif($activeTab === 'preview')
            @php($preview=$this->preview)<section class="ve-panel"><div class="ve-panel-head"><div><div class="ve-panel-title">Frontend readiness</div><div class="ve-note">Canonical catalog summary.</div></div></div><div class="ve-editor"><div class="ve-main"><pre class="ve-code">{{ json_encode($preview,JSON_PRETTY_PRINT|JSON_UNESCAPED_SLASHES) }}</pre></div><aside class="ve-preview"><h3>{{ $preview['title'] }}</h3>@foreach($preview['meta'] as $label=>$value)<div class="ve-preview-row"><span>{{ ucfirst((string)$label) }}</span><strong>{{ $value }}</strong></div>@endforeach</aside></div></section>
        @else
            @php($records=$activeTab==='videos'?$this->videos:($activeTab==='channels'?$this->channels:$this->playlists))
            <section class="ve-panel">
                <div class="ve-panel-head"><div><div class="ve-panel-title">{{ ucfirst($activeTab) }}</div><div class="ve-note">{{ $activeTab==='videos'?'Long-form video library':($activeTab==='channels'?'Organize video channels.':'Curate ordered playlists.') }}</div></div><button class="ve-btn primary" wire:click="{{ $activeTab==='videos'?'createVideo':($activeTab==='channels'?'createChannel':'createPlaylist') }}">+ Add</button></div>
                <div class="ve-toolbar"><div class="ve-filters"><input class="ve-input" wire:model.live.debounce.300ms="search" placeholder="Search"><select class="ve-select" wire:model.live="statusFilter"><option value="all">All status</option><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select>@if($activeTab==='videos')<select class="ve-select" wire:model.live="sourceFilter"><option value="all">All sources</option>@foreach(['uploaded_video','external_video','hls','youtube_video','web_embed'] as $source)<option value="{{ $source }}">{{ str_replace('_',' ',ucfirst($source)) }}</option>@endforeach</select>@endif</div><div class="ve-view"><button class="{{ $libraryView==='grid'?'active':'' }}" wire:click="setLibraryView('grid')">Grid</button><button class="{{ $libraryView==='list'?'active':'' }}" wire:click="setLibraryView('list')">List</button></div></div>
                @if(count($records))<div class="ve-library {{ $libraryView }}">@foreach($records as $record)@php($thumb=$this->publicThumbnailUrl($record->thumbnailMediaAsset??null))<article class="ve-card"><div class="ve-thumb">@if($thumb)<img src="{{ $thumb }}" alt="">@else▶@endif</div><div class="ve-card-body"><div class="ve-card-title">{{ $record->title }}</div><div class="ve-meta"><span class="ve-badge">{{ $record->status }}</span>@if($activeTab==='videos')<span class="ve-badge">{{ $record->source_type }}</span><span class="ve-badge">{{ optional($record->channel)->title }}</span>@if($record->duration_seconds)<span class="ve-badge">{{ gmdate('i:s',$record->duration_seconds) }}</span>@endif@if($record->is_live)<span class="ve-badge">Live</span>@endif@elseif($activeTab==='channels')<span class="ve-badge">{{ $record->videos_count }} videos</span><span class="ve-badge">{{ $record->playlists_count }} playlists</span>@else<span class="ve-badge">{{ optional($record->channel)->title }}</span><span class="ve-badge">{{ $record->items_count }} videos</span>@if($record->provider)<span class="ve-badge">{{ $record->provider }}</span>@endif@endif@if($record->is_featured)<span class="ve-badge">Featured</span>@endif</div></div><div class="ve-card-action"><button class="ve-btn" wire:click="{{ $activeTab==='videos'?'editVideo':($activeTab==='channels'?'editChannel':'editPlaylist') }}({{ $record->id }})">Edit</button>@if($activeTab==='videos')<button class="ve-btn" wire:click="previewVideo({{ $record->id }})">Preview</button>@endif</div></article>@endforeach</div>@else<div class="ve-empty">No matching {{ $activeTab }}.</div>@endif
            </section>
        @endif
    </div>
</x-filament-panels::page>
