<x-filament::page>
@php
    $app = $this->activeApp;
    $stats = $this->stats ?? [];
    $users = collect($this->users ?? []);
    $devices = collect($this->devices ?? []);
    $comments = collect($this->comments ?? []);
    $likes = collect($this->likes ?? []);
    $follows = collect($this->follows ?? []);
    $rules = collect($this->rules ?? []);
    $tabs = [
        'overview' => 'Overview',
        'users' => 'Users',
        'devices' => 'Devices',
        'likes' => 'Likes',
        'comments' => 'Comments',
        'follows' => 'Follows',
        'rules' => 'Rules',
    ];
    $activeTab = strtolower((string) request('tab', 'overview'));
    if (! array_key_exists($activeTab, $tabs)) $activeTab = 'overview';
@endphp

<style>
.au-shell{display:grid;gap:14px;padding-bottom:28px}.au-hero{border:1px solid rgba(34,211,238,.18);border-radius:26px;padding:20px;background:radial-gradient(circle at top left,rgba(34,211,238,.17),transparent 32%),radial-gradient(circle at top right,rgba(168,85,247,.15),transparent 36%),linear-gradient(135deg,rgba(2,6,23,.97),rgba(15,23,42,.88))}.au-hero-grid{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:18px;align-items:center}.au-kicker{display:inline-flex;align-items:center;gap:7px;min-height:28px;border:1px solid rgba(34,211,238,.32);background:rgba(34,211,238,.10);color:#cffafe;border-radius:999px;padding:6px 11px;font-size:11px;font-weight:950;text-transform:uppercase;letter-spacing:.08em}.au-title{margin:11px 0 0;color:#fff;font-size:clamp(28px,4vw,42px);line-height:1.03;font-weight:950;letter-spacing:-.055em}.au-sub{margin-top:9px;max-width:920px;color:rgba(255,255,255,.66);font-size:13px;line-height:1.6}.au-action-row{display:flex;gap:8px;flex-wrap:wrap;margin-top:14px}.au-app-card{min-width:230px;border:1px solid rgba(255,255,255,.10);border-radius:20px;background:rgba(2,6,23,.42);padding:15px}.au-app-card span{display:block;color:rgba(255,255,255,.50);font-size:11px;font-weight:900;text-transform:uppercase;letter-spacing:.08em}.au-app-card strong{display:block;color:#fff;margin-top:6px;font-size:18px}.au-tabs{display:flex;gap:8px;flex-wrap:wrap;border:1px solid rgba(255,255,255,.08);border-radius:20px;padding:9px;background:rgba(15,23,42,.78)}.au-tab{display:inline-flex;align-items:center;gap:7px;min-height:36px;border:1px solid rgba(255,255,255,.09);background:rgba(255,255,255,.045);color:rgba(255,255,255,.74);border-radius:999px;padding:8px 12px;font-size:12px;font-weight:900;text-decoration:none}.au-tab:hover,.au-tab.active{border-color:rgba(34,211,238,.45);background:linear-gradient(135deg,rgba(124,58,237,.80),rgba(6,182,212,.72));color:#fff}.au-count{display:inline-flex;align-items:center;justify-content:center;min-width:22px;height:22px;border-radius:999px;background:rgba(255,255,255,.10);font-size:10px}.au-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.au-stat{border:1px solid rgba(255,255,255,.09);border-radius:20px;background:rgba(15,23,42,.72);padding:14px}.au-stat span{display:block;color:#93c5fd;font-size:11px;font-weight:950;text-transform:uppercase;letter-spacing:.07em}.au-stat strong{display:block;color:#fff;font-size:27px;line-height:1;margin-top:10px}.au-stat small{display:block;color:rgba(255,255,255,.58);font-size:11px;margin-top:8px}.au-panel{border:1px solid rgba(255,255,255,.09);border-radius:24px;background:rgba(15,23,42,.72);padding:14px}.au-panel-head{display:flex;gap:12px;align-items:start;justify-content:space-between;margin-bottom:12px}.au-panel-title{color:#fff;font-size:18px;font-weight:950;letter-spacing:-.035em}.au-note{color:#93c5fd;font-size:12px;line-height:1.5;margin-top:5px}.au-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.au-two{display:grid;grid-template-columns:1fr 1fr;gap:12px}.au-card,.au-row{border:1px solid rgba(255,255,255,.09);border-radius:18px;background:rgba(2,6,23,.36);padding:12px}.au-card{display:grid;gap:9px}.au-card-title{color:#fff;font-size:14px;font-weight:950}.au-muted{color:rgba(255,255,255,.58);font-size:12px;line-height:1.45}.au-row{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:start;margin-bottom:9px}.au-mini{display:flex;gap:8px;flex-wrap:wrap;align-items:center}.au-avatar{width:40px;height:40px;border-radius:14px;display:flex;align-items:center;justify-content:center;background:linear-gradient(135deg,rgba(34,211,238,.22),rgba(124,58,237,.24));border:1px solid rgba(255,255,255,.10);color:#fff;font-weight:950}.au-pill{display:inline-flex;align-items:center;min-height:24px;border:1px solid rgba(255,255,255,.09);border-radius:999px;padding:5px 8px;background:rgba(255,255,255,.05);color:rgba(255,255,255,.72);font-size:10px;font-weight:950;text-transform:uppercase;letter-spacing:.04em}.au-pill.good{border-color:rgba(34,197,94,.28);background:rgba(34,197,94,.12);color:#bbf7d0}.au-pill.warn{border-color:rgba(245,158,11,.28);background:rgba(245,158,11,.12);color:#fde68a}.au-pill.danger{border-color:rgba(239,68,68,.30);background:rgba(239,68,68,.12);color:#fecaca}.au-pill.info{border-color:rgba(34,211,238,.30);background:rgba(34,211,238,.12);color:#cffafe}.au-btn{display:inline-flex;align-items:center;justify-content:center;min-height:34px;border:1px solid rgba(255,255,255,.10);border-radius:12px;background:rgba(255,255,255,.055);color:rgba(255,255,255,.84);padding:7px 10px;font-size:12px;font-weight:900;text-decoration:none;cursor:pointer}.au-btn.primary{border-color:rgba(34,211,238,.38);background:linear-gradient(135deg,rgba(124,58,237,.88),rgba(6,182,212,.78));color:#fff}.au-btn.good{border-color:rgba(34,197,94,.28);background:rgba(34,197,94,.12);color:#bbf7d0}.au-btn.warn{border-color:rgba(245,158,11,.28);background:rgba(245,158,11,.12);color:#fde68a}.au-btn.danger{border-color:rgba(239,68,68,.32);background:rgba(239,68,68,.10);color:#fecaca}.au-input{width:100%;border:1px solid rgba(255,255,255,.10);background:#020817;color:#fff;border-radius:14px;min-height:42px;padding:10px 12px;font-size:13px}.au-comment{margin-top:8px;border-radius:14px;background:rgba(255,255,255,.045);padding:10px;color:rgba(255,255,255,.78);font-size:12px;line-height:1.55}.au-token{max-width:320px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;color:rgba(255,255,255,.54);font-size:11px}.au-empty{border:1px dashed rgba(255,255,255,.14);border-radius:18px;background:rgba(2,6,23,.26);padding:15px;color:rgba(255,255,255,.62);font-size:13px}.au-rule{border-top:4px solid rgba(34,211,238,.55)}.au-rule.good{border-top-color:#22c55e}.au-rule.warn{border-top-color:#f59e0b}.au-rule.danger{border-top-color:#ef4444}@media(max-width:1180px){.au-stats{grid-template-columns:repeat(2,minmax(0,1fr))}.au-grid{grid-template-columns:repeat(2,minmax(0,1fr))}.au-two{grid-template-columns:1fr}}@media(max-width:760px){.au-hero-grid{grid-template-columns:1fr}.au-app-card{min-width:0}.au-tabs{overflow-x:auto;flex-wrap:nowrap}.au-tab{flex:0 0 auto}.au-stats,.au-grid{grid-template-columns:1fr}.au-row{grid-template-columns:1fr}.au-panel-head{display:grid}.au-token{max-width:240px}}
</style>

@if (! $app)
    <div class="au-shell"><section class="au-panel"><div class="au-panel-title">No Active App Selected</div><div class="au-note">Choose an active app from the workspace switcher first.</div></section></div>
@else
<div class="au-shell">
    <section class="au-hero">
        <div class="au-hero-grid">
            <div>
                <div class="au-kicker">Viewer Accounts + Interactions</div>
                <h1 class="au-title">User & Interaction Center</h1>
                <div class="au-sub">Manage signed-in app users, linked devices, comments, likes, follows, and the login rules that protect deeper interactions while browsing remains public.</div>
                <div class="au-action-row"><a class="au-btn" href="{{ url('/admin/moderation') }}">Open Moderation</a><a class="au-btn" href="{{ url('/admin/analytics-dashboard') }}">Open Analytics</a><button type="button" class="au-btn primary" wire:click="refreshCenter">Refresh</button></div>
            </div>
            <div class="au-app-card"><span>Active App</span><strong>{{ $app['name'] }}</strong><div class="au-muted">{{ $app['slug'] }} · App ID {{ $app['id'] }}</div></div>
        </div>
    </section>

    <nav class="au-tabs">
        @foreach($tabs as $key => $label)
            @php $count = match($key){'users'=>(int)($stats['users']??0),'devices'=>(int)($stats['devices']??0),'likes'=>(int)($stats['likes']??0),'comments'=>(int)($stats['comments']??0),'follows'=>(int)($stats['follows']??0),default=>null}; @endphp
            <a class="au-tab {{ $activeTab === $key ? 'active' : '' }}" href="{{ url('/admin/app-users?tab=' . $key) }}">{{ $label }}@if(!is_null($count))<span class="au-count">{{ number_format($count) }}</span>@endif</a>
        @endforeach
    </nav>

    @if($activeTab === 'overview')
        <section class="au-stats">
            <div class="au-stat"><span>App Users</span><strong>{{ number_format((int)($stats['users'] ?? 0)) }}</strong><small>Signed-in viewers scoped to this app.</small></div>
            <div class="au-stat"><span>Active Devices</span><strong>{{ number_format((int)($stats['active_devices'] ?? 0)) }}</strong><small>{{ number_format((int)($stats['devices'] ?? 0)) }} total device token(s).</small></div>
            <div class="au-stat"><span>Likes</span><strong>{{ number_format((int)($stats['likes'] ?? 0)) }}</strong><small>Protected by login token.</small></div>
            <div class="au-stat"><span>Comments</span><strong>{{ number_format((int)($stats['comments'] ?? 0)) }}</strong><small>{{ number_format((int)($stats['pending_comments'] ?? 0)) }} pending · {{ number_format((int)($stats['hidden_comments'] ?? 0)) }} hidden.</small></div>
            <div class="au-stat"><span>Follows</span><strong>{{ number_format((int)($stats['follows'] ?? 0)) }}</strong><small>Viewer-to-viewer/creator follow records.</small></div>
            <div class="au-stat"><span>Device Users</span><strong>{{ number_format((int)($stats['linked_device_users'] ?? 0)) }}</strong><small>Devices linked to signed-in users.</small></div>
            <div class="au-stat"><span>Browse Rule</span><strong>Public</strong><small>Watching and reading stay open.</small></div>
            <div class="au-stat"><span>Interaction Rule</span><strong>Login</strong><small>Like, comment, save, follow, report.</small></div>
        </section>
        <section class="au-two">
            <div class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Latest Users</div><div class="au-note">Recent signed-in viewers for this app.</div></div><a class="au-btn" href="{{ url('/admin/app-users?tab=users') }}">View Users</a></div>@forelse($users->take(6) as $user)<div class="au-row"><div class="au-mini"><div class="au-avatar">{{ mb_substr($user['name'],0,1) }}</div><div><div class="au-card-title">{{ $user['name'] }}</div><div class="au-muted">{{ $user['email'] }}</div></div></div><span class="au-pill info">{{ $this->formatDate($user['created_at']) }}</span></div>@empty<div class="au-empty">No signed-in app users found yet.</div>@endforelse</div>
            <div class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Latest Comments</div><div class="au-note">Comment actions are login-protected and app-scoped.</div></div><a class="au-btn" href="{{ url('/admin/app-users?tab=comments') }}">View Comments</a></div>@forelse($comments->take(5) as $comment)<div class="au-row"><div><div class="au-card-title">{{ $comment['post_title'] }}</div><div class="au-muted">{{ $comment['user_name'] }} · {{ $this->formatDate($comment['created_at']) }}</div><div class="au-comment">{{ \Illuminate\Support\Str::limit($comment['body'], 120) }}</div></div><span class="{{ $this->statusPill($comment['status']) }}">{{ $comment['status'] }}</span></div>@empty<div class="au-empty">No comments yet.</div>@endforelse</div>
        </section>
    @elseif($activeTab === 'users')
        <section class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Signed-in App Users</div><div class="au-note">Viewer accounts are separate from AppsHub admin/staff accounts. Search is scoped to the active app.</div></div><input class="au-input" style="max-width:360px" type="search" wire:model.live.debounce.400ms="search" placeholder="Search name or email..."></div>@forelse($users as $user)<article class="au-row"><div><div class="au-mini"><div class="au-avatar">{{ mb_substr($user['name'],0,1) }}</div><div><div class="au-card-title">{{ $user['name'] }}</div><div class="au-muted">{{ $user['email'] }}</div></div></div><div class="au-mini" style="margin-top:10px"><span class="au-pill">{{ $user['devices'] }} device(s)</span><span class="au-pill good">{{ $user['likes'] }} like(s)</span><span class="au-pill info">{{ $user['comments'] }} comment(s)</span><span class="au-pill">{{ $user['follows'] }} follow(s)</span></div></div><div class="au-muted">Joined<br><strong style="color:#fff">{{ $this->formatDate($user['created_at']) }}</strong></div></article>@empty<div class="au-empty">No users found for this app.</div>@endforelse</section>
    @elseif($activeTab === 'devices')
        <section class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Device Tokens</div><div class="au-note">Device tokens support push notifications. Some devices may be guest-only until the viewer signs in.</div></div></div>@forelse($devices as $device)<article class="au-row"><div><div class="au-mini"><span class="{{ $device['is_active'] ? 'au-pill good' : 'au-pill danger' }}">{{ $device['is_active'] ? 'Active' : 'Inactive' }}</span><span class="au-pill info">{{ $device['platform'] }}</span><span class="au-pill">{{ $this->formatDate($device['last_seen_at']) }}</span></div><div class="au-card-title" style="margin-top:9px">{{ $device['user_name'] }}</div><div class="au-muted">{{ $device['user_email'] ?: 'No signed-in user linked' }}</div><div class="au-token">{{ $device['token'] }}</div></div><div class="au-mini">@if($device['is_active'])<button type="button" class="au-btn danger" wire:click="deactivateDevice({{ (int)$device['id'] }})">Deactivate</button>@else<button type="button" class="au-btn good" wire:click="activateDevice({{ (int)$device['id'] }})">Activate</button>@endif</div></article>@empty<div class="au-empty">No device tokens recorded for this app yet.</div>@endforelse</section>
    @elseif($activeTab === 'comments')
        <section class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Comments</div><div class="au-note">Quick moderation tools for user comments. Full moderation remains available in Moderation.</div></div><a class="au-btn" href="{{ url('/admin/moderation') }}">Open Moderation</a></div>@forelse($comments as $comment)<article class="au-row"><div><div class="au-card-title">{{ $comment['post_title'] }}</div><div class="au-muted">{{ $comment['user_name'] }}{{ $comment['user_email'] ? ' · '.$comment['user_email'] : '' }}{{ $comment['post_bucket'] ? ' · '.$comment['post_bucket'] : '' }}</div><div class="au-comment">{{ $comment['body'] }}</div><div class="au-mini" style="margin-top:9px"><span class="{{ $this->statusPill($comment['status']) }}">{{ $comment['status'] }}</span><span class="au-pill">{{ $this->formatDate($comment['created_at']) }}</span></div></div><div class="au-mini"><button type="button" class="au-btn good" wire:click="publishComment({{ (int)$comment['id'] }})">Publish</button><button type="button" class="au-btn warn" wire:click="pendingComment({{ (int)$comment['id'] }})">Pending</button><button type="button" class="au-btn danger" wire:click="hideComment({{ (int)$comment['id'] }})">Hide</button></div></article>@empty<div class="au-empty">No comments found.</div>@endforelse</section>
    @elseif($activeTab === 'likes')
        <section class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Likes</div><div class="au-note">Recent content likes. Frontend should require login before this action.</div></div></div>@forelse($likes as $like)<article class="au-row"><div><div class="au-card-title">{{ $like['post_title'] }}</div><div class="au-muted">{{ $like['user_name'] }}{{ $like['user_email'] ? ' · '.$like['user_email'] : '' }}{{ $like['post_bucket'] ? ' · '.$like['post_bucket'] : '' }}</div></div><span class="au-pill good">{{ $this->formatDate($like['created_at']) }}</span></article>@empty<div class="au-empty">No likes recorded yet.</div>@endforelse</section>
    @elseif($activeTab === 'follows')
        <section class="au-panel"><div class="au-panel-head"><div><div class="au-panel-title">Follows</div><div class="au-note">Recent follow relationships for this app.</div></div></div>@forelse($follows as $follow)<article class="au-row"><div><div class="au-card-title">{{ $follow['follower_name'] }} follows {{ $follow['following_name'] }}</div><div class="au-muted">{{ $follow['follower_email'] }} → {{ $follow['following_email'] }}</div></div><span class="au-pill info">{{ $this->formatDate($follow['created_at']) }}</span></article>@empty<div class="au-empty">No follows recorded yet.</div>@endforelse</section>
    @else
        <section class="au-panel"><div class="au-panel-title">Interaction Rules</div><div class="au-note">These rules guide frontend behavior. Backend APIs already protect interaction endpoints with signed-in user tokens.</div></section>
        <section class="au-grid">@foreach($rules as $rule)<article class="au-card au-rule {{ $rule['tone'] ?? '' }}"><div class="au-card-title">{{ $rule['title'] }}</div><div class="au-muted">{{ $rule['body'] }}</div></article>@endforeach</section>
    @endif
</div>
@endif
</x-filament::page>
