<x-filament::page>
@php
    $activeTab = $this->activeTab ?? 'overview';
    $workspaceMode = $this->workspaceMode ?? 'index';
    $tabs = [
        'overview' => 'Overview',
        'roles' => 'Roles',
        'admin_users' => 'Admin Users',
        'access_preview' => 'Access Preview',
        'matrix' => 'Permissions Matrix',
        'rules' => 'Rules',
    ];
    $stats = $this->stats ?? [];
    $roles = collect($this->roles ?? []);
    $adminUsers = collect($this->adminUsers ?? []);
    $apps = collect($this->apps ?? []);
    $permissionGroups = $this->permissionGroups ?? [];
    $rules = collect($this->rules ?? []);
    $roleMap = $roles->keyBy('key');
    $isRoleEditor = $workspaceMode === 'role_editor';
    $isUserEditor = $workspaceMode === 'user_editor';
@endphp

<style>
    .rp-shell{display:grid;gap:14px;padding-bottom:32px}.rp-hero{border:1px solid rgba(34,211,238,.18);border-radius:26px;padding:20px;background:radial-gradient(circle at top left,rgba(34,211,238,.16),transparent 30%),radial-gradient(circle at top right,rgba(168,85,247,.16),transparent 34%),linear-gradient(135deg,rgba(2,6,23,.98),rgba(15,23,42,.94));box-shadow:0 18px 50px rgba(0,0,0,.26)}.rp-hero-grid{display:grid;grid-template-columns:minmax(0,1fr) minmax(230px,300px);gap:14px;align-items:center}.rp-kicker{display:inline-flex;align-items:center;gap:8px;border:1px solid rgba(34,211,238,.35);background:rgba(8,145,178,.18);color:#bff8ff;border-radius:999px;padding:7px 12px;font-weight:900;font-size:11px;text-transform:uppercase;letter-spacing:.06em}.rp-title{font-size:34px;line-height:1.04;font-weight:1000;color:#fff;margin:12px 0 8px}.rp-desc,.rp-muted,.rp-note{color:#a8c7e8;line-height:1.55}.rp-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}.rp-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;border:1px solid rgba(148,163,184,.22);background:rgba(15,23,42,.86);color:#fff;border-radius:13px;padding:10px 14px;font-weight:900;font-size:12px;text-decoration:none;cursor:pointer}.rp-btn.primary{background:linear-gradient(135deg,#7c3aed,#06b6d4);border-color:rgba(34,211,238,.35)}.rp-btn.danger{border-color:rgba(248,113,113,.42);background:rgba(127,29,29,.18);color:#fecaca}.rp-btn.good{border-color:rgba(16,185,129,.4);background:rgba(6,78,59,.25);color:#bbf7d0}.rp-btn.small{padding:7px 10px;font-size:11px;border-radius:10px}.rp-mini-card{border:1px solid rgba(148,163,184,.14);background:rgba(15,23,42,.6);border-radius:18px;padding:16px}.rp-tabs{display:flex;gap:8px;flex-wrap:wrap;border:1px solid rgba(148,163,184,.14);background:rgba(15,23,42,.85);border-radius:22px;padding:9px}.rp-tab{display:inline-flex;align-items:center;gap:8px;border:1px solid transparent;background:rgba(15,23,42,.8);color:#b7d7f7;border-radius:16px;padding:11px 14px;font-weight:900;text-decoration:none;font-size:13px}.rp-tab.active{background:linear-gradient(135deg,#7c3aed,#06b6d4);color:#fff}.rp-count{display:inline-grid;place-items:center;min-width:24px;height:24px;border-radius:999px;background:rgba(255,255,255,.12)}.rp-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:10px}.rp-stat,.rp-panel,.rp-card,.rp-role-card,.rp-user-row,.rp-rule,.rp-matrix-row{border:1px solid rgba(148,163,184,.14);background:linear-gradient(180deg,rgba(15,23,42,.92),rgba(2,6,23,.86));border-radius:20px;padding:15px}.rp-stat span{display:block;color:#93c5fd;font-size:11px;font-weight:1000;text-transform:uppercase;letter-spacing:.08em}.rp-stat strong{display:block;color:#fff;font-size:28px;margin-top:8px}.rp-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px}.rp-two{display:grid;grid-template-columns:1fr 1fr;gap:12px}.rp-panel-head,.rp-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap;margin-bottom:12px}.rp-panel-title,.rp-card-title{font-size:17px;font-weight:1000;color:#fff}.rp-filterbar{display:grid;grid-template-columns:1.4fr .8fr .8fr auto;gap:10px;margin:12px 0}.rp-input,.rp-select,.rp-textarea{width:100%;border:1px solid rgba(148,163,184,.22)!important;background:#07111f!important;color:#fff!important;border-radius:14px!important;padding:12px!important;min-height:44px}.rp-textarea{min-height:92px}.rp-select{appearance:auto!important}.rp-user-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.rp-user-main{display:flex;gap:12px;align-items:center}.rp-avatar{width:44px;height:44px;border-radius:15px;display:grid;place-items:center;background:linear-gradient(135deg,#06b6d4,#7c3aed);font-weight:1000;color:#fff}.rp-mini,.rp-role-actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:10px}.rp-pill{display:inline-flex;align-items:center;gap:6px;border:1px solid rgba(148,163,184,.22);background:rgba(15,23,42,.75);border-radius:999px;padding:6px 9px;font-size:11px;font-weight:900;color:#dbeafe}.rp-pill.good{background:rgba(6,78,59,.26);border-color:rgba(16,185,129,.36);color:#bbf7d0}.rp-pill.danger{background:rgba(127,29,29,.2);border-color:rgba(248,113,113,.36);color:#fecaca}.rp-pill.info,.rp-pill.cyan{background:rgba(8,47,73,.28);border-color:rgba(34,211,238,.32);color:#cffafe}.rp-pill.warn{background:rgba(113,63,18,.22);border-color:rgba(251,191,36,.35);color:#fde68a}.rp-pill.purple,.rp-pill.custom{background:rgba(88,28,135,.22);border-color:rgba(168,85,247,.35);color:#e9d5ff}.rp-role-top{height:50px;border-radius:15px;background:linear-gradient(135deg,rgba(34,211,238,.28),rgba(124,58,237,.22));margin-bottom:12px}.rp-permissions{display:flex;gap:6px;flex-wrap:wrap;margin-top:10px}.rp-matrix{display:grid;gap:10px}.rp-matrix-row{display:grid;grid-template-columns:210px minmax(0,1fr);gap:12px}.rp-focus{min-height:calc(100vh - 80px);display:grid;grid-template-columns:minmax(0,1fr) minmax(310px,420px);gap:14px}.rp-focus-main,.rp-focus-side{border:1px solid rgba(34,211,238,.18);background:linear-gradient(145deg,#07111f,#0b1728);border-radius:24px;padding:18px}.rp-focus-side{position:sticky;top:18px}.rp-form-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.rp-check-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.rp-check{display:flex;align-items:center;gap:8px;border:1px solid rgba(148,163,184,.15);background:rgba(2,6,23,.35);border-radius:14px;padding:10px;color:#dbeafe;font-weight:800;font-size:12px}.rp-app-list{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.rp-preview-list{display:grid;gap:8px}.rp-preview-row{display:flex;justify-content:space-between;gap:10px;border:1px solid rgba(148,163,184,.14);background:rgba(2,6,23,.35);border-radius:14px;padding:10px}.rp-ok{color:#86efac;font-weight:900}.rp-no{color:#fecaca;font-weight:900}.rp-empty{border:1px dashed rgba(148,163,184,.25);border-radius:18px;padding:18px;color:#93a4bd;text-align:center}.rp-role-card.inactive{opacity:.62}.rp-role-card.protected{border-color:rgba(248,113,113,.24)}
    @media(max-width:1100px){.rp-stats{grid-template-columns:repeat(2,1fr)}.rp-grid,.rp-user-list{grid-template-columns:repeat(2,1fr)}.rp-filterbar,.rp-two,.rp-focus{grid-template-columns:1fr}.rp-focus-side{position:relative;top:auto}.rp-matrix-row{grid-template-columns:1fr}}
    @media(max-width:680px){.rp-hero-grid,.rp-stats,.rp-grid,.rp-user-list,.rp-form-grid,.rp-check-grid,.rp-app-list{grid-template-columns:1fr}.rp-title{font-size:28px}.rp-tabs{overflow-x:auto;flex-wrap:nowrap}.rp-tab{white-space:nowrap}}
</style>

<div class="rp-shell" x-data="{restore(){let y=sessionStorage.getItem('rp_scroll'); if(y){setTimeout(()=>window.scrollTo(0, parseInt(y)),60)}}}" x-init="restore()" @beforeunload.window="sessionStorage.setItem('rp_scroll', window.scrollY)">
    @if($isRoleEditor)
        <section class="rp-focus">
            <div class="rp-focus-main">
                <div class="rp-panel-head"><div><span class="rp-kicker">Staff Role</span><div class="rp-title" style="font-size:26px">{{ ($this->editingRoleKey ?? 'new') === 'new' ? 'Create Custom Role' : 'Edit Custom Role' }}</div><div class="rp-desc">Create real staff roles for your team. System templates stay protected; custom roles are editable.</div></div><a class="rp-btn" href="{{ url('/admin/roles-permissions?tab=roles') }}">← Back to Roles</a></div>
                <div class="rp-form-grid">
                    <label><span class="rp-note">Role Name</span><input class="rp-input" wire:model.defer="roleForm.name" placeholder="Example: Dunamis Content Staff"></label>
                    <label><span class="rp-note">Role Key</span><input class="rp-input" wire:model.defer="roleForm.key" placeholder="dunamis_content_staff"></label>
                </div>
                <div style="margin-top:10px"><label><span class="rp-note">Description</span><textarea class="rp-textarea" wire:model.defer="roleForm.description" placeholder="What should this staff role do?"></textarea></label></div>
                <div class="rp-panel" style="margin-top:12px"><div class="rp-card-title">Permissions</div><div class="rp-note">Select exactly what this role should be allowed to use later when enforcement is enabled.</div>
                    @foreach($permissionGroups as $group => $permissions)
                        <div style="margin-top:12px"><div class="rp-card-title" style="font-size:13px;color:#bfdbfe">{{ $group }}</div><div class="rp-check-grid" style="margin-top:8px">@foreach($permissions as $permission)<label class="rp-check"><input type="checkbox" wire:model.defer="roleForm.permissions" value="{{ $permission }}"><span>{{ $permission }}</span></label>@endforeach</div></div>
                    @endforeach
                </div>
                <div class="rp-panel" style="margin-top:12px"><div class="rp-card-title">Default App Scope</div><div class="rp-note">Optional. User-specific app assignment can still override this when creating/editing a staff account.</div><div class="rp-app-list" style="margin-top:8px">@foreach($apps as $app)<label class="rp-check"><input type="checkbox" wire:model.defer="roleForm.assigned_app_ids" value="{{ $app['id'] }}"><span>{{ $app['name'] }}</span></label>@endforeach</div></div>
                <div class="rp-actions"><label class="rp-check"><input type="checkbox" wire:model.defer="roleForm.is_active"><span>Active role</span></label><button type="button" class="rp-btn primary" wire:click="saveRole">Save Role</button><a class="rp-btn" href="{{ url('/admin/roles-permissions?tab=roles') }}">Cancel</a></div>
            </div>
            <aside class="rp-focus-side"><div class="rp-card-title">Role creation guide</div><div class="rp-muted" style="margin-top:8px">Use this for real staff access such as Dunamis Content Staff, Celebration Moderator, Push Notification Staff, Video Manager, or Support Agent.</div><div class="rp-mini"><span class="rp-pill info">No lockout yet</span><span class="rp-pill custom">Custom Role</span></div></aside>
        </section>
    @elseif($isUserEditor)
        <section class="rp-focus">
            <div class="rp-focus-main">
                <div class="rp-panel-head"><div><span class="rp-kicker">Admin User</span><div class="rp-title" style="font-size:26px">{{ ($this->editingUserId ?? 0) > 0 ? 'Edit Staff Account' : 'Create Staff Account' }}</div><div class="rp-desc">Create backend staff login accounts, assign roles, set passwords, and restrict apps.</div></div><a class="rp-btn" href="{{ url('/admin/roles-permissions?tab=admin_users') }}">← Back to Admin Users</a></div>
                <div class="rp-form-grid">
                    <label><span class="rp-note">Name</span><input class="rp-input" wire:model.defer="userForm.name" placeholder="Staff name"></label>
                    <label><span class="rp-note">Email/Login</span><input class="rp-input" wire:model.defer="userForm.email" placeholder="staff@example.com"></label>
                    <label><span class="rp-note">Password {{ ($this->editingUserId ?? 0) > 0 ? '(leave blank to keep current)' : '' }}</span><input class="rp-input" type="password" wire:model.defer="userForm.password" placeholder="Set password"></label>
                    <label><span class="rp-note">Role</span><select class="rp-select" wire:model.defer="userForm.admin_role">@foreach($roles as $role)<option value="{{ $role['key'] }}">{{ $role['label'] }}</option>@endforeach</select></label>
                </div>
                <div class="rp-panel" style="margin-top:12px"><div class="rp-card-title">Assigned Apps</div><div class="rp-note">A staff user assigned to one app should later see only that app’s allowed tools.</div><div class="rp-app-list" style="margin-top:8px">@foreach($apps as $app)<label class="rp-check"><input type="checkbox" wire:model.defer="userForm.assigned_app_ids" value="{{ $app['id'] }}"><span>{{ $app['name'] }}</span></label>@endforeach</div></div>
                <div class="rp-actions"><label class="rp-check"><input type="checkbox" wire:model.defer="userForm.admin_is_active"><span>Active admin account</span></label><button type="button" class="rp-btn primary" wire:click="saveAdminUser">Save Admin User</button><a class="rp-btn" href="{{ url('/admin/roles-permissions?tab=admin_users') }}">Cancel</a></div>
            </div>
            <aside class="rp-focus-side"><div class="rp-card-title">Staff account rule</div><div class="rp-muted" style="margin-top:8px">This creates backend/admin users, not mobile app viewers. Viewer accounts remain inside App Users.</div><div class="rp-mini"><span class="rp-pill good">Backend Staff</span><span class="rp-pill info">App Scoped</span></div></aside>
        </section>
    @else
        <section class="rp-hero">
            <div class="rp-hero-grid"><div><span class="rp-kicker">System admin layer</span><h1 class="rp-title">Roles & Permissions</h1><div class="rp-desc">Create staff roles, create admin users, assign apps, set passwords, and preview access before enforcement. App Users remains the separate frontend viewer layer.</div><div class="rp-actions"><a class="rp-btn primary" href="{{ url('/admin/roles-permissions?tab=roles&workspace=role_editor&role=new') }}">+ Create Role</a><a class="rp-btn" href="{{ url('/admin/roles-permissions?tab=admin_users&workspace=user_editor&user=new') }}">+ Create Admin User</a><button type="button" class="rp-btn" wire:click="refreshCenter">Refresh</button></div></div><div class="rp-mini-card"><div class="rp-note">Phase</div><div class="rp-card-title">Phase 2: Role Manager</div><div class="rp-muted">Safe foundation. Page lockout comes after access preview testing.</div></div></div>
        </section>
        <nav class="rp-tabs">
            @foreach($tabs as $key => $label)
                @php $count = match($key){'roles'=>$stats['roles'] ?? null,'admin_users'=>$stats['shown_users'] ?? null,'matrix'=>$stats['permission_groups'] ?? null, default=>null}; @endphp
                <a class="rp-tab {{ $activeTab === $key ? 'active' : '' }}" href="{{ url('/admin/roles-permissions?tab=' . $key) }}">{{ $label }}@if(!is_null($count))<span class="rp-count">{{ number_format($count) }}</span>@endif</a>
            @endforeach
        </nav>

        @if($activeTab === 'overview')
            <section class="rp-stats">
                <div class="rp-stat"><span>Total Users</span><strong>{{ number_format($stats['total_users'] ?? 0) }}</strong><small class="rp-muted">Backend user records</small></div>
                <div class="rp-stat"><span>Assigned Admins</span><strong>{{ number_format($stats['assigned_admins'] ?? 0) }}</strong><small class="rp-muted">Have admin role</small></div>
                <div class="rp-stat"><span>Super Admins</span><strong>{{ number_format($stats['super_admins'] ?? 0) }}</strong><small class="rp-muted">Owner access</small></div>
                <div class="rp-stat"><span>Custom Roles</span><strong>{{ number_format($stats['custom_roles'] ?? 0) }}</strong><small class="rp-muted">Real staff roles</small></div>
            </section>
            <section class="rp-two"><div class="rp-panel"><div class="rp-panel-title">Layer separation</div><div class="rp-note">Roles & Permissions controls backend staff accounts. App Users controls mobile app viewers, guest devices, comments, likes, follows, and interaction rules.</div></div><div class="rp-panel"><div class="rp-panel-title">Next enforcement step</div><div class="rp-note">After creating real roles and staff users, we will enforce sidebar/page access so each staff member sees only assigned apps and permitted tools.</div></div></section>
        @elseif($activeTab === 'roles')
            <section class="rp-panel"><div class="rp-panel-head"><div><div class="rp-panel-title">Roles</div><div class="rp-note">System templates are protected. Use custom roles for real staff access.</div></div><a class="rp-btn primary" href="{{ url('/admin/roles-permissions?tab=roles&workspace=role_editor&role=new') }}">+ Create Custom Role</a></div></section>
            <section class="rp-grid">
                @foreach($roles->where('key','!=','unassigned') as $role)
                    <article class="rp-role-card {{ ($role['system'] ?? false) ? 'protected' : '' }} {{ !($role['is_active'] ?? true) ? 'inactive' : '' }}">
                        <div class="rp-role-top"></div><div class="rp-card-title">{{ $role['label'] }}</div><div class="rp-muted">{{ $role['description'] }}</div>
                        <div class="rp-mini"><span class="rp-pill {{ $role['tone'] ?? '' }}">{{ count($role['permissions'] ?? []) }} permission(s)</span><span class="rp-pill {{ ($role['system'] ?? false) ? 'danger' : 'custom' }}">{{ ($role['system'] ?? false) ? 'Protected Template' : 'Custom Role' }}</span><span class="rp-pill info">{{ $this->roleAssignedCount($role['key']) }} assigned</span></div>
                        <div class="rp-actions">@if(!($role['system'] ?? false))<a class="rp-btn small" href="{{ url('/admin/roles-permissions?tab=roles&workspace=role_editor&role=' . urlencode($role['key'])) }}">Edit</a><button type="button" class="rp-btn danger small" wire:click="deleteCustomRole('{{ $role['key'] }}')">Delete</button>@else<span class="rp-note">System role cannot be deleted.</span>@endif</div>
                    </article>
                @endforeach
            </section>
        @elseif($activeTab === 'admin_users')
            <section class="rp-panel"><div class="rp-panel-head"><div><div class="rp-panel-title">Admin Users</div><div class="rp-note">Create staff accounts, assign roles, set passwords, and assign apps.</div></div><a class="rp-btn primary" href="{{ url('/admin/roles-permissions?tab=admin_users&workspace=user_editor&user=new') }}">+ Create Admin User</a></div><div class="rp-filterbar"><input class="rp-input" type="search" wire:model.live.debounce.400ms="search" placeholder="Search name or email..."><select class="rp-select" wire:model.live="roleFilter"><option value="all">All Roles</option>@foreach($roles as $role)<option value="{{ $role['key'] }}">{{ $role['label'] }}</option>@endforeach</select><select class="rp-select" wire:model.live="statusFilter"><option value="all">All Status</option><option value="active">Active</option><option value="inactive">Inactive</option></select><button type="button" class="rp-btn" wire:click="refreshCenter">Refresh</button></div></section>
            <section class="rp-user-list">
                @forelse($adminUsers as $user)
                    <article class="rp-user-row"><div class="rp-user-main"><div class="rp-avatar">{{ mb_substr($user['name'],0,1) }}</div><div><div class="rp-card-title">{{ $user['name'] }}</div><div class="rp-muted">{{ $user['email'] }}</div></div></div><div class="rp-mini"><span class="rp-pill {{ $this->roleTone($user['role_key']) }}">{{ $user['role_label'] }}</span><span class="{{ $user['admin_is_active'] ? 'rp-pill good' : 'rp-pill danger' }}">{{ $user['admin_is_active'] ? 'Active' : 'Inactive' }}</span><span class="rp-pill info">{{ $user['permission_count'] }} permission(s)</span></div><div class="rp-muted">Assigned apps: {{ $user['assigned_app_names'] }}</div><div class="rp-muted">Created: {{ $this->formatDate($user['created_at']) }}</div><div class="rp-role-actions"><a class="rp-btn small" href="{{ url('/admin/roles-permissions?tab=admin_users&workspace=user_editor&user=' . $user['id']) }}">Edit Account</a><a class="rp-btn small" href="{{ url('/admin/roles-permissions?tab=access_preview&preview_user=' . $user['id']) }}">Preview Access</a>@if($user['admin_is_active'])<button type="button" class="rp-btn danger small" wire:click="deactivateAdmin({{ (int)$user['id'] }})">Deactivate</button>@else<button type="button" class="rp-btn good small" wire:click="activateAdmin({{ (int)$user['id'] }})">Activate</button>@endif</div></article>
                @empty
                    <div class="rp-empty">No admin users found.</div>
                @endforelse
            </section>
        @elseif($activeTab === 'access_preview')
            @php $preview = $this->accessPreview ?? []; @endphp
            <section class="rp-panel"><div class="rp-panel-head"><div><div class="rp-panel-title">Access Preview</div><div class="rp-note">Check what a staff user should see before we enforce sidebar/page restrictions.</div></div><select class="rp-select" style="max-width:360px" wire:model.live="previewUserId">@foreach($adminUsers as $user)<option value="{{ $user['id'] }}">{{ $user['name'] }} — {{ $user['role_label'] }}</option>@endforeach</select></div></section>
            <section class="rp-two"><div class="rp-panel"><div class="rp-panel-title">Should See</div><div class="rp-note">Assigned apps: {{ $preview['assigned_apps'] ?? '—' }}</div><div class="rp-preview-list" style="margin-top:10px">@forelse(($preview['allowed'] ?? []) as $item)<div class="rp-preview-row"><span>{{ $item['label'] }} <small class="rp-muted">{{ $item['scope'] }}</small></span><span class="rp-ok">Allowed</span></div>@empty<div class="rp-empty">No allowed modules.</div>@endforelse</div></div><div class="rp-panel"><div class="rp-panel-title">Should Not See</div><div class="rp-note">These should be hidden or blocked during enforcement.</div><div class="rp-preview-list" style="margin-top:10px">@forelse(($preview['blocked'] ?? []) as $item)<div class="rp-preview-row"><span>{{ $item['label'] }} <small class="rp-muted">{{ $item['scope'] }}</small></span><span class="rp-no">Blocked</span></div>@empty<div class="rp-empty">Nothing blocked for this user.</div>@endforelse</div></div></section>
        @elseif($activeTab === 'matrix')
            <section class="rp-panel"><div class="rp-panel-title">Permissions Matrix</div><div class="rp-note">Permission keys used by roles. Enforcement will use these groups in the next phase.</div></section><section class="rp-matrix">@foreach($permissionGroups as $group => $permissions)<article class="rp-matrix-row"><div><div class="rp-card-title">{{ $group }}</div><div class="rp-muted">{{ count($permissions) }} permission(s)</div></div><div class="rp-permissions">@foreach($permissions as $permission)<span class="rp-pill info">{{ $permission }}</span>@endforeach</div></article>@endforeach</section>
        @else
            <section class="rp-grid">@foreach($rules as $rule)<article class="rp-rule"><div class="rp-card-title">{{ $rule['title'] }}</div><div class="rp-muted" style="margin-top:8px">{{ $rule['body'] }}</div></article>@endforeach</section>
        @endif
    @endif
</div>
</x-filament::page>
